 != null) {                        mCallback.onItemAction(mVideo, VideoMenuCallback.ACTION_REMOVE);                    }                    mDialogPresenter.closeDialog();                }));    }    private void appendMarkAsWatchedButton() {    private void appendMarkAsWatchedButton() {        if (mVideo == null || !mVideo.hasVideo() || !mIsMarkAsWatchedButtonEnabled) {            return;        }        mDialogPresenter.appendSingleButton(                UiOptionItem.from(getContext().getString(R.string.mark_as_watched), optionItem -> {                    performMarkAsWatched(mVideo);                    mDialogPresenter.closeDialog();                })        );    }        mDialogPresenter.appendSingleButton(                UiOptionItem.from(getContext().getString(R.string.mark_as_watch
